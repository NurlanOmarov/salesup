-- Рынок корпоративного клиента. Доступ от домена не зависит (одна база на четыре
-- домена), но готовые сообщения работникам должны вести на домен своего рынка:
-- казахстанскому клиенту — study.activesales.kz, а не канонический .by.
-- Существующие организации остаются с NULL и читаются как домен по умолчанию.
ALTER TABLE "Organization" ADD COLUMN "site" TEXT;
