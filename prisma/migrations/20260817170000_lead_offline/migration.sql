-- Формат обучения в заявке: онлайн-доступ или живой корпоративный тренинг.
--
-- Офлайн платформа не продаёт: у такой заявки нет ни тарифа, ни расчёта мест, и
-- в админке по ней нечего создавать — это разговор про выездной тренинг. Без
-- отдельного поля обе истории смешивались бы в одном списке, и владелец узнавал
-- бы формат только из текста сообщения.

-- CreateEnum
CREATE TYPE "LeadFormat" AS ENUM ('ONLINE', 'OFFLINE');

-- AlterTable
ALTER TABLE "Lead" ADD COLUMN IF NOT EXISTS "format" "LeadFormat" NOT NULL DEFAULT 'ONLINE';
