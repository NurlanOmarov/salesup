-- Подписи работников B2B больше не шифруются в браузере ПИН-кодом: ответственный
-- пишет их открытым текстом (кличка, должность), владелец платформы их видит.
-- На проде к моменту миграции не было ни одной зашифрованной подписи и ни одного
-- ПИН-кода, поэтому колонка просто переименовывается, а старые значения (если
-- где-то остались) стираются — это нечитаемый шифротекст.
ALTER TABLE "OrgMembership" RENAME COLUMN "labelEnc" TO "label";
UPDATE "OrgMembership" SET "label" = NULL WHERE "label" IS NOT NULL;

DROP TABLE "OrgKeyWrap";
