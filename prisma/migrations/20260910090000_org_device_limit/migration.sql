-- Лимит устройств на уровне корпоративного клиента: у работников персональной
-- настройки обычно нет, а договориться о числе устройств клиент хочет разом на
-- всю компанию. NULL читается как стандартный лимит платформы (DEVICE_LIMIT),
-- 0 — безлимит, N — ровно столько устройств. Персональный лимит работника
-- (User.deviceLimit) остаётся сильнее этого.
ALTER TABLE "Organization" ADD COLUMN "deviceLimit" INTEGER;
