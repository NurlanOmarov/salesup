-- Самозапись работников по кодам убрана: работника заводит ответственный в
-- кабинете, платформа сразу выдаёт логин и временный пароль. Второй двери в
-- систему (страница /join) больше нет, поэтому и таблица кодов не нужна.
--
-- Данных, которые стоило бы сохранить, здесь не остаётся: активированные коды
-- уже превратились в учётки (Enrollment + OrgMembership), а неактивированные
-- перестали работать вместе с /join.
DROP TABLE IF EXISTS "OrgInvite";
