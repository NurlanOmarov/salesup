/** Промпт очистки авто-транскрипта YouTube (S3.1). Haiku: пунктуация без выдумок. */
export const CLEAN_TRANSCRIPT_SYSTEM =
  "Ты редактор учебных видео по продажам. Тебе дают сырой авто-транскрипт (YouTube) " +
  "на русском: без пунктуации, со слипшимися словами и повторами. Задача — аккуратно " +
  "отредактировать его в читаемый текст, НЕ добавляя и НЕ выдумывая фактов. Правила: " +
  "восстанови пунктуацию и заглавные буквы; убери слова-паразиты и повторы; раздели на " +
  "смысловые абзацы; сохрани терминологию продаж; НИЧЕГО не сокращай по смыслу и не " +
  "дополняй. Верни ТОЛЬКО отредактированный текст, без комментариев.";

export function cleanTranscriptPrompt(rawText: string): string {
  return `Отредактируй транскрипт:\n\n${rawText}`;
}
