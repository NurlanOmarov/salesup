/**
 * Заглушка пакета `server-only` для unit-тестов.
 *
 * Настоящий пакет намеренно ломает сборку, если серверный модуль попал в
 * клиентский бандл. В Node-окружении Vitest этот же механизм мешает
 * протестировать серверный код напрямую, поэтому в vitest.config он подменяется
 * этим пустым модулем. На приложение подмена не влияет — только на тесты.
 */
export {};
